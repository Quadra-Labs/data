node_modules
dist
*.log

# Off-chain index (npm run indexer) SQLite files.
*.db
*.db-wal
*.db-shm

# Local secrets / config (incl. .env.bak) — keep .env.example tracked.
.env*
!.env.example

# local-only seed scripts (may contain keys) — never commit
*.local.ts
